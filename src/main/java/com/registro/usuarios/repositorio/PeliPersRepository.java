package com.registro.usuarios.repositorio;
/* 
import com.registro.usuarios.modelo.PeliPersModel;
import org.springframework.stereotype.Repository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.CrudRepository;
import org.springframework.data.repository.query.Param;

@Repository
public interface PeliPersRepository extends CrudRepository<PeliPersModel, Long > {
 
    @Query(value="DELETE pelicula_personaje WHERE pel_id=:perid AND pel_id=:perid", nativeQuery = true)
    public void DeleteByIdID(@Param("pelid , perid") Long pelid , Long perid );

   }

   */